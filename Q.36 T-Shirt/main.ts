// 36. T-Shirt: Write a function called make_shirt() that accepts a size and the
// text of a message that should be printed on the shirt. The function should print
// a sentence summarizing the size of the shirt and the message printed on it.
// Call the function.

function make_shirt(size: number , lable: string){
    // size + lable;
     console.log( `My shirt size ${size} and lable is ${lable}`);
    
}

 make_shirt (30, " The T shirt is looking good quality" );
 make_shirt (36, " The T shirt is looking good quality" );
 make_shirt (40, " The T shirt is looking good quality" );
    
